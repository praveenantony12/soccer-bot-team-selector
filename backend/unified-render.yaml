services:
  # Frontend + Backend Service
  - type: web
    name: soccer-bot-team-selector
    runtime: node
    plan: free
    env: node
    rootDir: backend
    buildCommand: "cd .. && npm install --include=dev && npm run build:prod && cd backend && npm install --include=dev && npm run build"
    startCommand: "npm start"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: TZ
        value: America/New_York
      - key: TEAM_TIMEZONE
        value: America/New_York
      - key: TEAM_GENERATION_CRON
        value: 30 19 * * *
      - key: MIN_PLAYERS_TO_FORM_TEAMS
        value: 12
      - key: ENABLE_MANUAL_GENERATE
        value: false
